Apply package:
`boilerplate/update-packages/pz-boilerplate-intelliJ/2026-02-19-namespace-root-solutions-pointzero-symphony`

Target repo: `pz-boilerplate-intelliJ`.

Requirements:
1. Follow `apply-checklist.md`.
2. Enforce root namespace `solutions.pointzero.symphony`.
3. Reject legacy `com.upland.connect` references in active source/build/policy scope.
4. Run listed validation gates and report any non-trivial deltas.
